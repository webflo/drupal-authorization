authorization.settings:
  type: config_object
  label: 'Suthorization settings'
  mapping:
    authorization_message_on:
      type: boolean
      label: 'Show authorization and notification messages to the user.'
